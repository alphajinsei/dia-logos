# dia-logos — プロジェクトの引き継ぎドキュメント

別チャットの Claude が最初に読む前提で書いている。このファイルで思想・現状・次の一手・
守るべき一線を把握できるようにしてある。運用手順の詳細は [README.md](./README.md)、
記事化ルールの正本は [.claude/skills/kijika/SKILL.md](./.claude/skills/kijika/SKILL.md)。

## これは何か

ユーザーが Claude との対話から選び取った「思考の軌跡」を公開するブログ。
`https://dia-logos.alphajinsei.com` で**完全公開**（認証なし）。

名前 `dia-logos` の由来：διά「〜を通して」＋ λόγος「言葉・理」＝
「対話を通して理に至る」。この企画の本質を一語で表している。

## 起点は2つある（どちらも記事として公開済み）

- **2026-06-17「知性のきらめきを、どこに置くか」＝ 最初の思いつき。**
  「その場のニューロンの発火」を残したい、という動機から始まった。
  この対話で既に **①ラボノートと論文の二層構造（＝サマリー／軌跡）**、
  **②curation は redaction（伏字・除去）でもあるという警告（＝剪定必須ルール）** が示されていて、
  いまの設計はほぼそのまま従っている。
  また「唯一無二の思考」という自己評価は土台として弱く、**検証可能なのは
  「普段交わらない領域の語彙を移植する手つき」と「アナロジーを壊れるまで追い詰める規律」だ**、
  という批評を受けている（これが下記「残す対象の2軸」のB軸の原点）。
- **2026-07-06「知識の時代から選択の時代へ」＝ 情報理論からの基礎づけ。**
  知識がAIに集約された世界で希少なのは「何を選び取ったか」だ、という角度。

## なぜやるのか（思想 — ここを外すと的を外す）

1. **希少性の移動**：普遍的知識はAIに集約され誰でも取り出せる。だから「知っていること」は
   もう希少でない。希少なのは巨大な知の集合から**何を選び取ったか**、さらにその
   **選択の理由と軌跡**（点ではなく軌道）。
2. **本当の核＝AIによる思想の私的増幅への対抗**：みんながAIと話しすぎて各自の世界観を
   私的に増幅した結果、初対面での相互理解が難しくなった。自分の思考の軌跡を開いておくことで、
   考え方の癖・迷い方・何を面白がるかが伝わるようにする。エコーチェンバーの逆をやる。
3. **佇まいは受け身。差し出して回るものではない**（「名刺」という比喩は使わない — ユーザーの意図と逆）。
   興味を持って訪ねてきた人が読んで「ああこういうことを普段考えているんだな」と受け取ってくれれば
   それでいい、という姿勢。だから読まれ方を最適化しない（バズを意識した瞬間に平均＝スープに戻り、
   情報量が下がる）。押し出さず、置いておく。この受け身の姿勢が一貫性の核。

### 残す対象は2軸ある（選択の足跡 ＋ アナロジー）

このブログに残す価値があるのは、次の2種類。どちらもユーザーを特徴づける。

- **A. 選択の足跡**：巨大な知の集合から何を選び取ったか、その理由と軌跡（上記1の話）。
- **B. 異分野アナロジーの気づき**：ユーザーは多様なバックグラウンドを持ち、異分野の事象間に
  アナロジー（構造的な相似）を発見するのが得意だと自己評価している。この「私ならではの気づき」
  自体がユーザーを特徴づけるので、記事として残す。例：古代ローマの労働喪失×AI時代／
  がん細胞×システムアーキテクチャ／計算機のスタック×人間社会／RAG×attention。

記事化のとき、その対話が B（アナロジー）を含むなら、**どの分野とどの分野を、どんな構造で
橋渡ししたのか**が読者に伝わるように書く（アナロジーの骨格を潰さない）。tags に `アナロジー`
を付けると後で辿りやすい。

## 成果物の形（思想が構造に落ちている）

各記事＝1つの対話。**3層のセット**で、並置ではなく入れ子：

- **サマリー**（front matter の `summary`）＝**何の話だったか**。自分用の索引。結論の要約。
  単体で読めて、同時に本文への目次になる。
- **要点**（front matter の `notes`、任意）＝**何が分かったか**。内容そのものの結晶。
  議論の要約ではなく、構造として持ち帰れる形（箇条書き・対応表）で書く。
  ユーザー自身が対話後に整理したメモがあれば、それを最優先で使う。
- **軌跡**（本文）＝**どう考えたか**。他者向けの、生に近い対話ログ。ここが情報量の本体。
  **丸めない**。迷い・反論・逡巡をそのまま残す（均すと情報量が下がる）。
- 各記事に**日付**を刻む＝「その時点の私」のスナップショット。思想が後で変わっても記事は直さない。
  年輪のように、変化そのものを見せる。

**「要点」は当初なかった層で、4本目（がん細胞×システム設計）で追加した。** 長い対話を記事化して
みて、内容そのものを持ち帰る場所がないと分かったため。ユーザーの言葉：「軌跡とは別に、内容そのもの
に対する興味深さもあって、要約と本文の間くらいの粒度のものが欲しい」。企画の構造自体が、記事を
作りながら進化している（この改良ループ自体が運用の一部）。

## 守るべき一線（完全公開ゆえ絶対）

- **記事化時の個人情報の剪定は必須・妥協しない**。実名（本人・家族・第三者）、自宅が割れる地名、
  連絡先、ローカル絶対パス、認証情報、他プロジェクトの秘匿情報、本人がまだ見せたくない逡巡。
  迷ったら消す。剪定と誇張は別物：個人情報は削るが思考の生々しさは削らない。
- **生ログを公開リポに入れない**。`conversations.json` と `export/` は `.gitignore` 済み。
  リポに入るのは剪定済みの md だけ。

## 技術構成

```
[対話] --/記事化(Skill kijika)--> src/content/posts/*.md --git push--> [Cloudflare Pages が Astro をビルド] --> 公開
```

- **Astro**（静的サイト生成）＋ **Cloudflare Pages**（配信）＋ **GitHub**（真実のソース）
- リポ: `github.com/alphajinsei/dia-logos`（public）
- ドメイン: `alphajinsei.com` はお名前.com購入・CloudflareでDNS一元管理。
  サブドメイン `dia-logos.alphajinsei.com` を Pages のカスタムドメインに割当済み。
- **真実はこのリポの `src/content/posts/*.md`**。Astro もホストも「表示係」で丸ごと移設可能
  （＝データ主権はユーザーにある）。
- ページ: トップ（日付降順の索引）/ 記事ページ（サマリー→軌跡の入れ子）/ タグページ / RSS。
- `astro.config.mjs` の `site` は `https://dia-logos.alphajinsei.com`。

### 主要ファイル
- `src/content/config.ts` — 記事スキーマ（title / date / summary / tags / draft）。
  front matter はこれと一致させる。
- `src/pages/index.astro` / `posts/[...slug].astro` / `tags/[tag].astro` / `rss.xml.js`
- `src/layouts/Base.astro`, `src/styles/global.css`
- `.claude/skills/kijika/SKILL.md` — 記事化ルールの正本
- `scripts/split-export.mjs` — 過去ログ一括分割スクリプト

### ローカル
`npm install` → `npm run dev`（localhost:4321）／ `npm run build`（dist/ 出力）。
Node 22 / npm 10 / git は導入済み。`gh` CLI は**未導入**（GitHub操作はブラウザ＋手動 remote）。

## 会話履歴は2層に分かれている（記事の元ネタの所在）

記事の素材になる過去の対話は、**保存場所が2つに分かれている**。ここを取り違えると棚卸しが噛み合わない。

| 履歴の種類 | 保存場所 | 取得方法 |
|---|---|---|
| **claude.ai**（Webのチャット） | Anthropicサーバ | Export data（ユーザーが1回リクエスト → メールで `conversations.json`） |
| **Claude Code**（このセッション含む） | ローカル `~/.claude/projects/<proj>/*.jsonl` | Claude が直接読める（Export不要） |

新規の更新運用は場所を問わず kijika（対話中に「記事化して」）でその場変換する。
特に Claude Code の会話は Claude がその場で読めるので、Export を経由する必要はそもそもない。

### ⚠ Claude Code のローカル履歴は、既に失われている（2026-07-12 確認）

**ローカル履歴も棚卸しに盛り込むつもりだったが、過去分は全て消えていて盛り込めなかった。**

- `cleanupPeriodDays` の**既定値は 30 日**。それを過ぎたトランスクリプト（`*.jsonl`）は自動削除される。
- 確認時点で残っていた会話履歴は **7 セッションのみ**（この blog プロジェクト 6 件 ＋ kabu-api 1 件）。
- **RL（強化学習）等の過去プロジェクトの会話は全滅**。`memory/*.md`（作業メモの要約）だけが残っていた。
  会話そのものではないので記事化には薄く、断念した。
- **対策済み**：`~/.claude/settings.json` に `"cleanupPeriodDays": 3650` を設定した（10年保持）。
  今後の会話は残る。**この設定を消さないこと。**

参考：失われた RL プロジェクトの内容（memory から復元できた範囲）
— 4目並べのDQN。ステージ5でカリキュラム学習（noise 0.8→0.5→0.2→0.0 の相手強度を自動移行）、
攻防セットの中間報酬（自分の3連 +0.30 / 相手の3連を防いだ +0.50 / 相手の勝ち手を見逃した −0.80）を実装。
これは公開済み記事「相手が賢くなると、環境が変わってしまう」（2026-03-13）の**3日後の実装**にあたり、
理論→実装→改良のループが実際に回っていた証拠になっている。

## 進捗（2026-07-12 時点）— 棚卸しは完了。以後は新規運用フェーズ

**完成・稼働中：**
- [x] Astro サイト（一覧・日付索引・タグ・RSS・**3層構造**・数式描画 KaTeX）
- [x] `/記事化` Skill（kijika）＝変換ルール固定・剪定必須。**失敗のたびに改良を重ねた**
- [x] GitHub リポ `alphajinsei/dia-logos` / Cloudflare Pages / カスタムドメイン（稼働中）
- [x] **claude.ai の棚卸し完了**。全171件を確認し、**16記事を公開**
- [x] **Claude Code のローカル履歴の確認完了**（上記のとおり、過去分は消失していて対象なし）

**記事化を見送ったもの（方針として記録。蒸し返さない）：**
- **投資・株関連（約40件）** — 個人の資産情報が絡む
- **「AI時代における何を作るかの選択基準」** — 出身大学・研究室・**実名で検索可能な論文タイトル**を含み、
  一般化しても個人が特定される
- **生活実務（車・保育園・住宅・医療など約30件）／技術の調べ物（約20件）／時事解説（約10件）**
  — 思想やアナロジーが出る場面がない

**残（急がない）：**
- [ ] 将来やりたくなるかもしれない拡張：対話特有の見せ方（発言者ごとの吹き出し等）。
  Astro を選んだのはこの拡張余地のため（Hugo比）。

## 日々の運用（ユーザーの操作はこれだけ）

- 新規：記事にしたい対話中に「記事化して」と言う（Skill kijika 起動）→ 剪定報告を確認 → push。
- 過去分：上の「残」の手順。新規と同じ Skill ルールで記事化する。

## Cloudflare の詰まりどころ（次に触る人へ）

新規作成の導線が Workers 優先になっていて「アプリケーションを作成する」だと Workers に飛ぶ。
Pages を作るには Workers & Pages 画面 → 作成 → 下部の「Pages を導入しようとお考えですか？始める」
→「既存の Git リポジトリをインポートする」から入る。Framework preset に **Astro** が出れば正解の画面。
（このプロジェクトは連携済みなので、通常はもう触らない。push すれば自動で再ビルドされる。）
